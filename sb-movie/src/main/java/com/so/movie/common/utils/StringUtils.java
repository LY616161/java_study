package com.so.movie.common.utils;

public class StringUtils {
    public static boolean isEmpty(String str){
        if(str.length()==0||str=="")
            return true;
        else return false;
    }
}
